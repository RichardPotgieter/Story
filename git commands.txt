git init

git status

git add

git log
- shows history of saved changes

git commit -m "Present tense change"

git diff 
- Looks at differance of current file to previouse save state

git checkout 
- changes back to previous save state

git remote add origin "url"
- transfers logs to github

git push -u origin master
- must do this after "git remote add origin"
- pushes local repository to remote repository
- "origin" - remote
- "master" - name of branch

git branch "branch name"
- creates a new branch of repositories

git branch
- View git branches

git checkout "branch name"
- Switches you to a different branch